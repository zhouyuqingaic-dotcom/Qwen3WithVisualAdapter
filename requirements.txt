# RoMA-Net requirements
# Reported experiment environment:
#   Python: 3.11.15
#   CUDA: 13.0
#   GPU: 8 x NVIDIA A40
#
# Notes:
# 1. The reported experiments used PyTorch 2.10.0+cu130 and TorchVision 0.25.0+cu130.
#    If these CUDA-specific wheels are not available in your package index, install a
#    PyTorch/TorchVision build compatible with your local CUDA environment first.
# 2. The reported experiments used Transformers 5.3.0.dev0. If this exact dev build is
#    unavailable from PyPI, install the matching development version from source/nightly.
# 3. DeepSpeed and TRL are not required for the reported experiments.

torch==2.10.0+cu130
torchvision==0.25.0+cu130

transformers==5.3.0.dev0
peft==0.18.1
accelerate==1.13.0
bitsandbytes==0.49.2
datasets==4.7.0
sentencepiece==0.2.1
tokenizers==0.22.2
qwen-vl-utils==0.0.14

numpy==2.3.5
pandas==3.0.1
pillow==12.0.0
openai==2.29.0
tqdm==4.67.3

# Direct dependencies used by the code or required by the model-loading pipeline.
# Pin these after checking your local environment with `pip show <package>`.
safetensors
open-clip-torch

# Required when the Qwen3-VL loader uses attn_implementation="flash_attention_2".
# If flash-attn is not available on your system, set the attention implementation
# in the configuration/model loader to "sdpa" or "eager".
flash-attn